'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const fd=new FormData(e.currentTarget);const r=await fetch('/api/session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({phone:fd.get('phone'),password:fd.get('password')})});const d=await r.json().catch(()=>({detail:'The API returned an unreadable response'}));if(r.ok)router.replace('/');else setError(d.detail||'Unable to sign in')}catch{setError('Unable to reach the Haazir API. Try again.')}finally{setBusy(false)}}return <main className="login-shell"><section className="login-brand"><div className="mark">H</div><p>HAAZIR / OPERATIONS</p><h1>The control room for every service promise.</h1><span>Live marketplace visibility, careful intervention, accountable operations.</span></section><form className="login-card" onSubmit={submit}><div><small>SECURE ADMIN ACCESS</small><h2>Welcome back</h2><p>Use your authorized operations account.</p></div><label>Phone number<input name="phone" autoComplete="username" required placeholder="+91 98765 43210" /></label><label>Password<input name="password" type="password" autoComplete="current-password" required /></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Enter operations'}</button></form></main>}
